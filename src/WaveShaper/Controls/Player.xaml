<UserControl x:Class="WaveShaper.Controls.Player"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WaveShaper.Controls"
             mc:Ignorable="d" >
    <Grid>
        <GroupBox Header="Player" Margin="0,0,1,0" >
            <DockPanel>
                <Label Name="LblFileTitle">No file loaded...</Label>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <Label x:Name="LblTime" RenderTransformOrigin="1.526,1.526" Content="0:00 / 0:00" />
                    <ToggleButton x:Name="BtnPlay" Click="BtnPlay_OnClick" Content="▶" MinWidth="25" MinHeight="25"/>
                    <Button x:Name="BtnStop" Click="BtnStop_OnClick" Content="■" MinWidth="25" MinHeight="25"/>
                    <Button x:Name="BtnOpenFile" Click="BtnOpenFile_OnClick" Content="📂" MinWidth="25" MinHeight="25"/>
                </StackPanel>
            </DockPanel>
        </GroupBox>
    </Grid>
</UserControl>
